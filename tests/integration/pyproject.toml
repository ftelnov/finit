[project]
name = "finit-integration-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "psycopg2-binary>=2.9",
    "sseclient-py>=1.8",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["."]
